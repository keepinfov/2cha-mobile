package dev.yaul.twocha.protocol

/**
 * Display-level constants for the 2cha v4 client. The wire protocol itself lives
 * entirely in the native engine; these values are only used for UI labels.
 */
object Constants {
    /** Protocol version implemented by the native engine. */
    const val PROTOCOL_VERSION: Byte = 4
}
